{
    "name": "orieg/expanse",
    "description": "Native PHP bindings for Expanse via PIE / C ABI / FFI",
  "keywords": [
    "judy",
    "libjudy",
    "trie",
    "radix-tree",
    "ordered-map",
    "ffi",
    "data-structures"
  ],
    "type": "library",
    "license": "MIT OR Apache-2.0",
    "version": "0.5.0",
    "require": {
        "php": "^8.1 || ^8.2 || ^8.3 || ^8.4 || ^8.5",
        "ext-ffi": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0 || ^11.0"
    },
    "suggest": {
        "ext-expanse": "Install via 'pie install orieg/expanse-extension' for maximum native performance (falls back to FFI + libexpanse otherwise)."
    },
    "autoload": {
        "psr-4": {
            "Expanse\\": "src/"
        }
    }
}
